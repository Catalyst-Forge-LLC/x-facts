---
description: Deploy the static site with pnpm ship
alwaysApply: true
---

# Deploy

Site publish is `pnpm ship` (Wrangler Worker `x-facts`, uploads `site/` as static assets with no build step). This is a Worker, not a Pages project. Do not use `wrangler pages deploy`.
