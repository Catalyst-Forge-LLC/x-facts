# xFacts hub — light updates (2026-08)

> Hub only. Do not paste ToolFacts / AgentFacts / SkillFacts field taxonomies here.

**Status:** executed (2026-08-07).

---

## Review

The hub already tells the family story well (x is a variable; anatomy; admission
rule; footer ownership). Gaps:

1. Status table still says ToolFacts/AgentFacts “Spec in progress” and SkillFacts
   “Reserved” — understates local scaffold + this session’s flesh-out.
2. No agent pointer (`llms.txt`) to the five label domains.
3. README status table should match the landing page.

## Plan

1. Refresh status language on `site/index.html` and `README.md` to honest
   first-deployment readiness (spec/schema/examples/validator present; public
   deploy may still be pending).
2. Add `site/llms.txt` listing label URLs + what each answers (one line each) +
   pointer to each label’s `/llms.txt` when live.
3. Optionally mention the dual human/agent audience in one short line if missing.
4. Do **not** add format tutorials, schemas, or exemplar galleries to the hub.

## Acceptance

- [x] Status copy matches suite reality after flesh-out.
- [x] Hub `llms.txt` exists and stays thin.
- [x] No duplicated label meat.

## Completions

| Item | Status |
|---|---|
| Status copy | done |
| `site/llms.txt` | done |
| README sync | done |
